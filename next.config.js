/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["picsum.photos", "localhost", "circumicons.com"],
  },

  // experimental: {
  //   serverActions: true,
  // },
};

module.exports = nextConfig;
