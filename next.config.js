/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["picsum.photos", "localhost"],
  },
  // experimental: {
  //   serverActions: true,
  // },
};

module.exports = nextConfig;
